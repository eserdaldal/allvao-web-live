- name: 🚀 Spaceship (allvao_app) Hizli Senkronizasyon
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./
          server-dir: ./allvao_app/
          # 'dangerous-clean-slate' sunucudaki farklı olan her şeyi siler ve 
          # sadece GitHub'dakileri bırakır. En temiz yöntem budur:
          dangerous-clean-slate: false 
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.next/cache/**
            package-lock.json